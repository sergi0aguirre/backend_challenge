#contact_select
  = link_to "#{@ordertxt}","", :id => :orderlink
  = link_to "Select all","", :id => :selectall
  = link_to "Select none","", :id => :selectnone
  = link_to "Delete selected contacts","", :id => :deletecontacts
#contact_paginate
  =will_paginate @contacts, :params => {:key => params[:key],:order => params[:order],:action=>"index", :controller=>"contacts"}
  %br/
  =link_to "> Expor all to Vcards",export_to_vcards_contacts_path, :target => :blank
.clear_both
!=table_up('All my contacts')
.picture_column.table_headers
  Picture
.name_column.table_headers
  Full Name
.clear_both
=render @contacts
!=table_down
