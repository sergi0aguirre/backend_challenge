%div
  -unless params[:action]=="show"
    = link_to "Add new address",new_address_path(:contact_id => @contact_id || @contact.id), :remote => true
!=table_up('Addresses')
=render @addresses
!=table_down
